services:
  app:
    build: .
    ports:
      - "5000:5000"
    env_file:
      - .env
    environment:
      - DB_PATH=/app/data/knowledge_base.db
    volumes:
      - db_data:/app/data
    restart: unless-stopped

volumes:
  db_data:
